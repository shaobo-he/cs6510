#lang plai-typed
(require plai-typed/s-exp-match
         "class.rkt"
         "inherit.rkt"
         "typed-class.rkt"
         "inherit-parse.rkt"
         "typed-parse.rkt")

(module+ test
  (print-only-errors true))

(module+ test
  ;; 1. instanceof form
  
  ;; 

  
)
  
  ;; End 1. instanceof form
